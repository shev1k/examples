const namespace = 'planning';

export const SET_BENCHMARK = `${namespace}/SET_BENCHMARK`;

export const SET_BOARD = `${namespace}/SET_BOARD`;
export const UPDATE_BOARD = `${namespace}/UPDATE_BOARD`;

export const UPDATE_FILTER = `${namespace}/UPDATE_FILTER`;

export const SET_SELECTED_ISSUE = `${namespace}/SET_SELECTED_ISSUE`;
export const CLEAR_SELECTED_ISSUE = `${namespace}/CLEAR_SELECTED_ISSUE`;
