const { generateReleases, getReleaseBuffer } = require("./makeReleases");
const { generateMetadata, getMetadataBuffer } = require("./makeMetadata");

module.exports = {
  generateReleases,
  generateMetadata,
  getReleaseBuffer,
  getMetadataBuffer,
};
